import React from 'react';

import PostsList from './PostsList';

const Home = (props) => {
    return (
        <div>
            <div>

            </div>
            <PostsList />
        </div>
    );
};

export default Home;